apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-pvc-xfs-with-args
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-sc-xfs-with-args
